# D05｜3 候选评分

- 使用时机：圆桌后、拍板前
- 目标输出：逐项评分、硬淘汰、排序依据

## 变量区（复制后填写）

```yaml
PROMPT_ID: D05
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{三候选}}｜{{六指标证据}}｜{{核心美术数}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 只用六指标 100 分模型。
- 先硬淘汰。
- 缺证据降分并标待核验。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「3 候选评分」。读取变量区全部字段，以 逐项评分、硬淘汰、排序依据 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：只用六指标 100 分模型；先硬淘汰；缺证据降分并标待核验。
固定输出格式：Markdown 表：候选×六指标/贡献分/总分/淘汰原因；末尾给并列破局。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

Markdown 表：候选×六指标/贡献分/总分/淘汰原因；末尾给并列破局。不得追加与本任务无关的扩展方案。
