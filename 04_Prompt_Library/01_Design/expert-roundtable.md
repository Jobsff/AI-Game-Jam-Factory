# D04｜四专家交叉质疑

- 使用时机：已有恰好 3 候选
- 目标输出：共识、交叉质疑、致命风险、最小验证

## 变量区（复制后填写）

```yaml
PROMPT_ID: D04
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{决策器上下文}}｜{{三候选}}｜{{限制}}｜{{孩子判断}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 四专家必须互相引用和反驳。
- 不新增候选。
- 不替人拍板。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「四专家交叉质疑」。读取变量区全部字段，以 共识、交叉质疑、致命风险、最小验证 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：四专家必须互相引用和反驳；不新增候选；不替人拍板。
固定输出格式：严格输出：共识/专家交叉质疑/候选致命风险/5分钟验证/给孩子三题/建议排序。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

严格输出：共识/专家交叉质疑/候选致命风险/5分钟验证/给孩子三题/建议排序。不得追加与本任务无关的扩展方案。
