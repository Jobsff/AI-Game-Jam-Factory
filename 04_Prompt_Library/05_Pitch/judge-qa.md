# P04｜评委 Q&A

- 使用时机：路演彩排
- 目标输出：尖锐问题与证据化回答

## 变量区（复制后填写）

```yaml
PROMPT_ID: P04
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{GDD/TDD}}｜{{决策记录}}｜{{测试}}｜{{AI/儿童日志}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 不编造分数、奖项、用户数据。
- 未知直接待核验并给核验方法。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「评委 Q&A」。读取变量区全部字段，以 尖锐问题与证据化回答 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：不编造分数、奖项、用户数据；未知直接待核验并给核验方法。
固定输出格式：输出问题/20秒回答/证据路径/追问/禁答夸大点，至少覆盖玩法、AI、孩子、技术、license。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

输出问题/20秒回答/证据路径/追问/禁答夸大点，至少覆盖玩法、AI、孩子、技术、license。不得追加与本任务无关的扩展方案。
