# P03｜AI 使用说明

- 使用时机：提交文档阶段
- 目标输出：透明 AI 使用说明

## 变量区（复制后填写）

```yaml
PROMPT_ID: P03
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{AI usage log}}｜{{工具/模型}}｜{{采用/拒绝记录}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 区分人类输入/AI输出/人类决定。
- 版本不明标待核验。
- 不披露秘密。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「AI 使用说明」。读取变量区全部字段，以 透明 AI 使用说明 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：区分人类输入/AI输出/人类决定；版本不明标待核验；不披露秘密。
固定输出格式：输出工具表、用途、人工控制点、未采用内容、license/数据待核验项。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

输出工具表、用途、人工控制点、未采用内容、license/数据待核验项。不得追加与本任务无关的扩展方案。
