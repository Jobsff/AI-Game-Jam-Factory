# A06｜风格一致性审查

- 使用时机：每批资产导入前
- 目标输出：偏差与最小修正清单

## 变量区（复制后填写）

```yaml
PROMPT_ID: A06
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{Style Bible}}｜{{资产 manifest}}｜{{资产预览描述}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 不重做无关资产。
- 按轮廓/色板/材质/光照/UI逐项举证。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「风格一致性审查」。读取变量区全部字段，以 偏差与最小修正清单 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：不重做无关资产；按轮廓/色板/材质/光照/UI逐项举证。
固定输出格式：输出 asset id/偏差/严重度/修正 prompt/程序替代/是否阻断。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

输出 asset id/偏差/严重度/修正 prompt/程序替代/是否阻断。不得追加与本任务无关的扩展方案。
