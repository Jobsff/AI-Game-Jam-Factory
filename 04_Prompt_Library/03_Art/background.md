# A03｜背景资产

- 使用时机：核心场景确定后
- 目标输出：背景生成 prompt 与分层清单

## 变量区（复制后填写）

```yaml
PROMPT_ID: A03
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{Style Bible}}｜{{场景功能}}｜{{镜头}}｜{{可交互留白}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 不得抢夺可交互对象对比度。
- 不含受保护角色/标识。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「背景资产」。读取变量区全部字段，以 背景生成 prompt 与分层清单 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：不得抢夺可交互对象对比度；不含受保护角色/标识。
固定输出格式：输出构图、前中后景、色值、留白区、循环边缘、导出规格。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

输出构图、前中后景、色值、留白区、循环边缘、导出规格。不得追加与本任务无关的扩展方案。
