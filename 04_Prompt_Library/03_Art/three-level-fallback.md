# A07｜三级美术降级

- 使用时机：90 分钟计时点
- 目标输出：A/B/C 决策与替代映射

## 变量区（复制后填写）

```yaml
PROMPT_ID: A07
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{当前等级}}｜{{已用时间}}｜{{可导入资产}}｜{{阻断项}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 90分钟到点必须降级。
- 保玩法反馈。
- 不因快好了续时。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「三级美术降级」。读取变量区全部字段，以 A/B/C 决策与替代映射 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：90分钟到点必须降级；保玩法反馈；不因快好了续时。
固定输出格式：输出当前证据、触发条件、保留/替换/删除清单、30分钟执行序列。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

输出当前证据、触发条件、保留/替换/删除清单、30分钟执行序列。不得追加与本任务无关的扩展方案。
