# C01｜架构三层地图

- 使用时机：编码前
- 目标输出：体验/运行/实现地图与风险

## 变量区（复制后填写）

```yaml
PROMPT_ID: C01
THEME: "{{THEME}}"
GAME_NAME: "{{GAME_NAME}}"
INPUTS: "{{GDD}}｜{{TDD}}｜{{仓库树}}｜{{现有 API}}"
CONTEXT: "{{CONTEXT}}"
TIME_LIMIT: "{{TIME_LIMIT}}"
EVIDENCE_PATHS: "{{EVIDENCE_PATHS}}"
```

## 不可违反约束

- 只读分析。
- 先列拟改文件且≤3。
- 禁止跨模块隐改。
- 只依据变量区与明确证据；事实不确定处写“待核验”，并给核验动作。
- 不复制第三方代码、文案或素材。

## 可直接使用的 Prompt

```text
你负责「架构三层地图」。读取变量区全部字段，以 体验/运行/实现地图与风险 为目标。
先复述边界与缺失输入，再完成任务；缺失信息不得静默猜测。
必须遵守：只读分析；先列拟改文件且≤3；禁止跨模块隐改。
固定输出格式：输出三张表、依赖方向、所有权、首个模块建议、验证命令。
最后追加【自检清单】，逐项使用 - [ ] / - [x]，并标出所有“待核验”。
```

## 固定输出格式

输出三张表、依赖方向、所有权、首个模块建议、验证命令。不得追加与本任务无关的扩展方案。
