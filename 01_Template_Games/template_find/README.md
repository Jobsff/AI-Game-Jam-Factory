# 隐藏寻找母件

找齐所有目标，点错会反馈，超时失败。

## 运行

从仓库根目录运行 `npm run serve`，打开 `/01_Template_Games/template_find/`。

- 画布：720×1280，`FIT + CENTER_BOTH`。
- 运行时：Phaser 3、原生 JavaScript、ES Module、零 npm 依赖。
- 结束后点击画面即可重开。
