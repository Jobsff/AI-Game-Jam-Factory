# 抉择叙事母件

完成 3 轮二选一，观察世界变化与结局。

## 运行

从仓库根目录运行 `npm run serve`，打开 `/01_Template_Games/template_choice/`。

- 画布：720×1280，`FIT + CENTER_BOTH`。
- 运行时：Phaser 3、原生 JavaScript、ES Module、零 npm 依赖。
- 结束后点击画面即可重开。
