# 核心防守母件

点击清除敌人，保护核心直到倒计时结束。

## 运行

从仓库根目录运行 `npm run serve`，打开 `/01_Template_Games/template_defense/`。

- 画布：720×1280，`FIT + CENTER_BOTH`。
- 运行时：Phaser 3、原生 JavaScript、ES Module、零 npm 依赖。
- 结束后点击画面即可重开。
